{
  "name": "dumahshop",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node app.js",
    "dev": "node scripts/prepare-source-html.mjs && vite",
    "build": "node scripts/prepare-source-html.mjs && node scripts/clean-dist.mjs && vite build && node scripts/prepare-worker.mjs && node scripts/publish-static.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.3.0"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.2"
  }
}
